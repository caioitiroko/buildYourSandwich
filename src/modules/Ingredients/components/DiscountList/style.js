import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    alignItems: "center",
  },
  discount: {
    fontWeight: "bold",
    color: "#999",
    fontSize: 16,
  }
});
