import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    alignItems: "center",
    backgroundColor: "#FFF",
    padding: 20,
    marginBottom: 30,
  },
  title: {
    color: "#000",
    fontSize: 20,
    fontWeight: "bold",
  },
});
