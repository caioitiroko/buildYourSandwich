import { StyleSheet } from "react-native";

export default StyleSheet.create({
  background: {
    alignItems: "center",
    height: "100%",
    width: "100%",
  },
  container: {
    alignItems: "center",
    flex: 1,
    justifyContent: "center",
  },
});
